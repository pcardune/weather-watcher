// import { fromJS } from 'immutable';
// import { makeSelectDatabaseDomain } from '../selectors';

// const selector = makeSelectDatabaseDomain();

describe('makeSelectDatabaseDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
